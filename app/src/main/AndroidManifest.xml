<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kaysersose.fiszki">

    <application
        android:name=".FiszkiApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".AddingDeckActivity" />
        <activity
            android:name=".AddingCardsActivity"
            android:label="@string/title_activity_adding_cards"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LearningFromDeckActivity"></activity>
    </application>

</manifest>